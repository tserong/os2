name: Dev/Daily Build and Publish

on:
  push:
    branches:
    - sle-micro
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

jobs:
  build-for-dev:
    uses: ./.github/workflows/factory.yml
    with:
      tag: ${{ github.ref_name }}-head
      push: true
    secrets: inherit